  <h1>Users</h1>
  <ul>
    <% @users.each do |user| %>
    <li><%= "Name: " + user.name + ": " + " | Mail: " + user.email + " | Birthday: " + user.birthday.strftime("%d/%m/%Y") %></li>
    <% end %>
  </ul>
