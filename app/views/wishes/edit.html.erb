<div class="content edit">

<%= render 'form' %>   <!-- Partial _form.html.erb -->

<% if @user == current_user %>
<button class="waves-effect waves-teal btn-flat"><%= link_to('Eliminar', user_wish_path(@user, @wish),
      method: :delete, data: { confirm: 'Are you sure to delete?' }) %></button>
      <% end %>
</div>
